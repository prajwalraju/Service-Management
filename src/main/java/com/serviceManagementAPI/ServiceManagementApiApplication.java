package com.serviceManagementAPI;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// main Spring boot application initializer
@SpringBootApplication
public class ServiceManagementApiApplication {

    // Main Spring boot application method
    public static void main(String[] args) {
        SpringApplication.run(ServiceManagementApiApplication.class, args);
    }
}
